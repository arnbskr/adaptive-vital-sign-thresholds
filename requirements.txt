google-cloud-bigquery
pandas
db-dtypes
numpy
scikit-learn
joblib
streamlit
pyarrow
python-dotenv
chromadb
pypdf
openai
# Phase 2: official MCP SDK. OPTIONAL — the default local in-process tool backend
# (src/mcp_server.py + src/tool_backends/local_backend.py) works without it.
# Install it to run the real MCP network backend: the streamable-http server
# (src/server_mcp.py) and the remote client backend (mcp_remote).
mcp
fastmcp